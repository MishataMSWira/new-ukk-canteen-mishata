// import express from "express"
// import { verifyToken } from "../middlewares/authorization"
// import { getAllMenuDiskon, createMenuDiskon, updateMenuDiskon, dropMenuDiskon } from "../controllers/menudiskonController";
// import { verifyAddDiskonMenu, verifyEditDiskonMenu } from "../middlewares/verifyMenuDiskon"



// const app = express()

// app.use(express.json())

// app.get(`/`,[verifyToken], getAllMenuDiskon)
// app.post(`/`, [verifyToken, verifyAddDiskonMenu], createMenuDiskon)
// app.put(`/:id`,[verifyToken, verifyEditDiskonMenu], updateMenuDiskon)
// app.delete(`/:id`, [verifyToken,], dropMenuDiskon)
// export default app